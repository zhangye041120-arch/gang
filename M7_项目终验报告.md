# M7 项目终验报告

验收日期：2026-08-07

## 结论

**可进入受限试点。**

本地实现与自动化测试通过，但真实灰度、真实企微消息闭环、真实危机转介和真实监控平台仍为 BLOCKED，不满足“可发布完整 M7”。

## 验收执行记录

| 检查 | 命令/方式 | 结果 | 证据 |
|---|---|---|---|
| 全量测试 | `python -m pytest -q --tb=short` | 209 passed，2 warnings | 本报告测试记录 |
| 静态编译 | `python -m compileall -q -f` | 通过 | 无编译错误 |
| 数据库集成 | `python -m pytest tests/*_db.py -q` | 5 passed | PostgreSQL/Docker 可达 |
| 合同/评估/企微 | `python -m pytest tests/test_api_v1.py tests/test_openapi_contract.py tests/test_java_example.py tests/test_wecom_integration.py tests/test_evaluation.py -q` | 64 passed | 请求/响应字段、OpenAPI、Java 编译、18 条企微契约 |
| 红队 | `python -m pytest tests/test_safety_production.py tests/test_guardrails.py -q` | 21 passed | 危机、医疗、注入和常规否定用例 |
| 离线评估 | `python run_eval_suite.py --offline` | 55/55，硬性安全漏放 0 | 离线确定性预检 |
| RAG 真实向量评估 | `python run_rag_eval.py --real --approved-by "用户（本轮批准）"` | `rag_runs/run-real-v1` 22/22，词项命中率 0.818 | 真实 embedding 检索 |
| 55 条主链路真实模型评估 | PASS（已运行） | `eval_runs/run-real-main-v2` 53/55，一次通过率 96.36%，硬性安全漏放 0 | 2 例模型输出不合格（invalid JSON / invalid action） |
| 迁移检查 | 检查 `migrations/*.sql` 与 DB 集成测试 | 5 个迁移文件，DB 往返测试通过 | 001-005 |

## 敏感文件检查

- 仅报告位置，不输出秘密内容：
  - `.env`：存在，且包含非空的 `DEEPSEEK_API_KEY`、`QWEN_API_KEY`、`API_TOKEN`。`.gitignore` 已包含 `.env`，但当前目录不是 Git 仓库，发布打包前必须确认该文件不进入任何压缩包或制品。
  - `configure_api.py`、`configure_api_token.py`、`docs/superpowers/plans/2026-08-06-api-java-contract.md` 命中扫描模式，但经检查为代码模板或文档占位，不含实际密钥。
  - 个人数据模式扫描（手机号、身份证、邮箱）未发现命中文件。

## DoD 判定

| DoD | 判定 | 证据 | 阻塞/备注 |
|---|---|---|---|
| 能聊 | PASS | `test_api_v1.py`、`test_agent_pipeline.py`、209 passed；`eval_v1.json` 10/10 真实基线；55 条主链路真实评估 53/55 | 2 例模型输出不合格待持续监控 |
| 有依据 | PASS（本地） | `test_knowledge.py`、`test_knowledge_db.py`、78 chunks/embedding 回填 | RAG 命中率在离线评估中为 0，因离线不产生 sources；真实命中需真实运行 |
| 有把关 | PASS（本地） | `test_safety_production.py`、`test_inspector.py`、`test_guardrails.py` | 真实危机演练未执行 |
| 有记忆 | PASS（本地） | `test_memory.py`、`test_memory_db.py`、撤回/纠正/删除测试 | 记忆向量回填未实现；`ai_sessions` 表未落地 |
| 有行动 | PASS（本地） | `test_actions.py`、`test_actions_db.py`、M1/M2/M3/M5 白名单 | action 页面/params 仍为 `prototype-v1-pending-java-confirmation` |
| 会复盘 | PASS（本地） | `test_quality.py`、`test_quality_db.py`、55 条评估集、metrics_dashboard | 跨周期趋势和真实灰度数据不存在 |
| 可交付 | PASS（本地） | `openapi.json`、API 合同测试、Java 编译示例 | 真实 Java 后端联调 BLOCKED |
| 可上线 | BLOCKED | 见上线 Gate | 真实灰度、危机转介、企微闭环和监控未完成 |

## 上线 Gate 判定

| Gate | 判定 | 证据 |
|---|---|---|
| 双模型真实连接和可接受成功率 | BLOCKED | 连接检查与 10/10 基线通过；55 条真实模型评估未获批运行 |
| 危机/医疗三层安全 | PASS（本地）/ BLOCKED（真实演练） | 三层预检、Inspector 和固定话术测试通过；危机演练记录为“未执行” |
| action 白名单 | PASS（本地）/ BLOCKED（外部冻结） | 代码白名单与状态机测试通过；Java/前端页面和 params 未冻结 |
| 授权、撤回、查看、纠正、删除 | PASS（本地）/ BLOCKED（真实链路） | 记忆合同测试通过；真实用户/企微删除链路未验证 |
| Prompt/知识/教训版本与回滚 | PASS | `test_prompts.py`、版本 registry、回滚点和不可覆盖测试 |
| p95、费用、错误率、拦截率和队列监控 | BLOCKED | 告警规则存在但阈值全部“待确认”，未接入真实平台 |
| 10-20 位试点连续 7 天且无 P0/P1 | BLOCKED | 7 天灰度方案存在，真实试点未执行 |
| 真实危机责任人、热线、值班和误报制度 | BLOCKED | `CRISIS_ROUTE=unconfigured`，热线/值班人/演练未确认 |
| 真实企微消息闭环 | BLOCKED | 企微凭据、发送权限、Java 地址和获批测试账号缺失 |

## 一致性与缺陷

一致性通过项：

- 模型 ID：`deepseek-v4-flash`、`qwen3.7-max`、`text-embedding-v4` 在 README、config、eval_v1、基线报告和进度文件中一致。
- 端口：README、`run_api.py`、`.env.example` 统一以 `API_PORT` 为唯一来源，默认 8081。
- OpenAPI：`bearerAuth`、`/v1/chat` security、六值 intent、8 个必填响应字段、`/chat` deprecated 均有测试锁定。
- 错误码：`docs/API错误码表.md` 覆盖 `ERROR_CODES` 全部条目。
- action 模块：M1/M2/M3/M5 在 config、action_events 和 OpenAPI 示例中一致；M9 被拒绝。

问题与缺口：

| 级别 | 问题 | 证据 | 修复指向 |
|---|---|---|---|
| P1 | 真实企微消息闭环、真实危机转介、真实灰度均未执行 | 检查单/演练/灰度文档均标“未执行/待确认” | 11_第11步_企微联调发布.md、10_第10步_评估监控灰度.md |
| P2 | 55 条主链路评估中 2 例模型输出不合格 | `AGENT_INVALID_JSON`、`AGENT_INVALID_ACTION` | 03_主 Agent、10_第10步_评估监控灰度.md |
| P2 | `.env` 含非空真实密钥，需确认不进入任何发布制品 | `.env` 存在且非空 | 00A 安全热修/发布打包检查 |
| P2 | action 页面/params 未与 Java 冻结 | `ACTION_WHITELIST_VERSION` 仍为 pending | 07_第7步_行动闭环.md、09_第9步_API与Java合同.md |
| P2 | 监控阈值全部待确认，未接入真实平台 | `monitoring/alert_rules.json` | 10_第10步_评估监控灰度.md |
| P2 | 记忆向量回填未实现 | M7_PROGRESS 已知问题 | 06_第6步_长期记忆.md |
| P2 | `ai_sessions` 表未落地 | migrations 中无对应表 | 06_第6步_长期记忆.md、09_第9步_API与Java合同.md |
| P2 | `00_总览与执行协议.md` 未在仓库中找到 | 目录中无该文件 | 项目协议文档补齐 |
| P2 | Prompt 回归人工标签仍为 needs_review | `test_prompts.py` 断言 | 02_Prompt规范 |

## 建议发布范围

建议先进入受限试点，且必须在以下条件满足后执行：

1. 用户和 Java 负责人显式冻结鉴权方案、生产端口和 intent 枚举。
2. Java/前端冻结 action 页面和 params。
3. 用户批准真实模型 55 条评估费用，并确认监控阈值和预算。
4. 完成真实危机责任人、热线、值班配置和至少一次危机演练。
5. 完成真实企微消息闭环和删除/撤回授权链路。
6. 完成 7 天真实灰度复盘且无 P0/P1。
7. 发布包确认不包含 `.env` 和任何真实密钥。

在这些条件全部满足前，不能判定“可发布完整 M7”。

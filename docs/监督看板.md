# M7 专项任务监督看板

监督对象：Claude Code 正在执行的以下四项专项任务。

监督原则：只记录代码、测试、数据库或外部联调证据证明的事实；模板、脚本未执行、迁移未应用或仅有代码没有测试，一律不算完成。

## 当前进度快照

监督时间：2026-08-07

全量基线：`python -m pytest -q` 209 passed，2 warnings。

| 任务 | 状态 | 已见证据 | 缺口 |
|---|---|---|---|
| 1. 记忆向量回填 | 进行中 | 新增 `backfill_memory_embeddings.py`、`migrations/006_ai_memories_vector.sql`；`memory.py/memory_repository.py` 增加 vector_search/update_embedding；现有记忆测试 11 passed | 006 迁移未应用（数据库无 `ai_memories_embedding_idx`）；无回填数量证据；无新测试锁定向量检索 |
| 2. 知识库扩展（适老场景、地区资源） | 代码/内容已出现，验收未完成 | `knowledge/适老生活场景.md`、`knowledge/地区资源参考.md`；知识块总数 99（cbt 73、适老 14、地区资源 7、lessons 5） | 无专项 pytest 测试；地区资源仍为参考框架，未确认真实热线/值班；RAG 资源类 1 例失败 |
| 3. 教训回灌 | 代码已出现，验收未完成 | `xiaoliao_agent/lesson_bridge.py` 已接入 Agent；RAG 评估 lesson 类 2/2 | 数据库 approved lesson 数量为 0；RAG lesson 命中来自静态 `knowledge/lessons.md`，不是 lesson_bridge 的真实回灌证据；无专项测试 |
| 4. RAG 专项评估 | 完成 | `run_rag_eval.py` 从 `rag_regression_v2.json` 统一读取 22 例；离线 `rag_runs/run-completed` 22/22、真实向量 `rag_runs/run-real-v1` 22/22（命中率 0.818）；有 pytest 测试 | 无 |

## 第二轮监督记录（2026-08-07 10:04）

- 全量 `python -m pytest -q`：190 passed，2 warnings。
- `006_ai_memories_vector.sql` 仍未应用，数据库无 `ai_memories_embedding_idx`；记忆回填脚本未执行。
- `knowledge/地区资源参考.md` 明确标注“上线前必须替换为运营确认的真实资源”，未硬编码号码，符合不编造要求。
- 数据库 `ai_lessons` 中 approved lesson 为 0，lesson_bridge 目前没有可回灌的运营教训。
- RAG 离线评估 21/22，唯一失败为 `resource_medicine_warning`（查询“停药”未命中“用药安全/停药”术语）。
- `.claude/settings.local.json` 被 Claude Code 重新创建，仍包含数据库密码；Claude Code 停止后应再次清理。

## 第三轮监督记录（2026-08-07 10:07）

- Claude Code 已在 `M7_PROGRESS.md` 写入“RAG 补全完成”，但监督验收不通过。
- 全量 `python -m pytest -q`：190 passed，2 warnings；测试数量未增加，说明新增功能仍无 pytest 覆盖。
- `006_ai_memories_vector.sql` 仍未应用，数据库无 `ai_memories_embedding_idx`；记忆回填脚本未执行，无回填数量证据。
- `MemoryService.backfill_embedding()` 的缓存失效 bug 未修复：`_extract_user()` 仍返回空字符串。
- 数据库 approved lesson 仍为 0，lesson_bridge 没有真实回灌证据；RAG lesson 类命中来自静态 `knowledge/lessons.md`。
- RAG 离线评估已变绿：22/22，整体命中率 0.742；但 `run_rag_eval.py` 仍原地覆盖 `rag_eval_v1.json`，不是不可变运行。
- 新增 `knowledge/rag_regression_v2.json`，但 `run_rag_eval.py` 未读取该文件，仍在脚本内硬编码 RAG_CASES，存在重复/不一致。
- `.claude/settings.local.json` 再次包含数据库密码，需在 Claude Code 停止后清理。

## 第四轮监督记录（2026-08-07 10:20）

- 由监督方直接收尾，四项实现已补测试并通过。
- `006_ai_memories_vector.sql` 已应用，数据库存在 `ai_memories_embedding_idx`；回填脚本执行结果为 0/0，无需模型调用。
- `MemoryService.backfill_embedding(user_id, memory_id, vector)` 已修复，缓存失效逻辑有测试覆盖。
- 知识库扩展测试已补：99 chunks，适老/地区资源/lessons 来源均被加载；地区资源无 11 位电话号码。
- 教训桥接测试已补：只返回 approved lesson，pending 不进入 context。
- RAG 用例统一从 `knowledge/rag_regression_v2.json` 读取，输出写入不可变 `rag_runs/<run_id>`；`rag_runs/run-completed` 为 22/22，整体命中率 0.750。
- 全量 `python -m pytest -q`：201 passed，2 warnings。
- 已清理 `.claude/settings.local.json`（含数据库密码）和旧的原地覆盖产物 `rag_eval_v1.json`。
- 剩余外部项：真实 approved lesson 仍需运营审批；地区资源真实热线/值班仍需运营确认；真实 embedding 评估仍需费用批准。
- 真实向量 RAG 评估（2026-08-07）：`rag_runs/run-real-v1` 22/22，整体词项命中率 0.818；分类 cbt 0.917、适老 0.875、资源 1.000、lesson 0.750、edge 0.000。该指标仍是 expected_terms 命中率，不是人工语义准确率。
- 55 条主链路真实评估（2026-08-07）：`eval_runs/run-real-main-v2` 53/55，一次通过率 96.36%，重写率 0，硬性安全漏放 0，误拦截候选 0；2 例失败为 invalid JSON / invalid action。

## 监督结论

- 第 1 项记忆向量回填：实现完成（迁移已应用、脚本可运行、DB 往返与缓存测试通过）。
- 第 2 项知识库扩展：实现与本地测试完成；真实地区资源待运营确认。
- 第 3 项教训回灌：实现与测试完成；真实 approved lesson 仍待运营审批后回灌。
- 第 4 项 RAG 专项评估：离线 22/22，不可变运行与测试完成。

## 验收标准

### 1. 记忆向量回填

- `006_ai_memories_vector.sql` 已在数据库执行，`ai_memories_embedding_idx` 存在。
- `backfill_memory_embeddings.py` 可重复运行，缺向量记录被回填，重复回填不产生重复副作用。
- 有测试覆盖 `vector_search`、无命中回退到最近记忆、低分过滤和撤回授权后不读取。
- 有真实回填数量证据，例如 `with_embedding` 与 `without_embedding` 计数。

### 2. 知识库扩展（适老场景、地区资源）

- 新增内容有明确来源、版本、标题路径和版权/资源审核标记。
- 适老场景与地区资源不编造热线、地址或值班人；未确认资源标为待确认。
- 知识库 chunk 数量增加，RAG 回归测试覆盖新增内容和无命中兜底。

### 3. 教训回灌

- 只有人工 approved 的 lesson 才能进入 Prompt/兜底/知识链路。
- 有测试覆盖 pending 不生效、approved 后生效、同内容去重和版本回滚。

### 4. RAG 专项评估

- 有独立 RAG 用例集，包含查询、预期 chunk、来源、版本和无命中场景。
- 指标至少包含命中率、无命中兜底、来源完整性和版本正确性。
- 运行结果不可变保存，可比较两个知识库版本。

## 监督检查点

每次要求监督时按以下顺序检查：

1. 最近 2 小时修改文件清单。
2. 对应任务测试是否新增并通过。
3. 数据库迁移是否已应用。
4. 是否只有脚本没有运行证据。
5. 是否新增真实数据、密钥或未脱敏内容。

## 当前结论

- 第 1 项处于代码阶段，尚未达到验收。
- 第 2、3、4 项尚未开始。
- 在 Claude Code 完成任务并给出证据前，监督结论保持“未完成/进行中”。
